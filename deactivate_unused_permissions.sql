-- Desactiva permisos que están en DB pero no se usan en código (generado automáticamente)
-- Recomendación: agrega primero la columna `status` con default 1.
--
-- ALTER TABLE us_permissions ADD COLUMN status TINYINT(1) NOT NULL DEFAULT 1;
--
-- Nota: revisa la lista antes de ejecutar en PROD; puede haber permisos usados dinámicamente.

-- No hay permisos para desactivar.
